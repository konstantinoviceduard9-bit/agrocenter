/**
 * Единые формулировки про демо-данные и дорожную карту (AGENTS.md, роль C).
 * Не дублировать длинно то, что уже показывает DataSourceStrip под шапкой.
 */
export const APP_COPY = {
  quarterFilterNote:
    'Квартал в шапке пока только подпись; срез метрик по кварталу появится вместе с API к учёту.',
  ledgerRoadmap: 'Боевой учёт (1С / банк) — на этапе интеграции; таблицы сейчас на демо-наполнении.',
  /** Короткая подсказка в сайдбаре (дублирует смысл серой полоски под шапкой, но в меню без полоски). */
  dataModeSidebar: 'Режим данных (мок / JSON) на каждой странице — в серой строке под шапкой.',
} as const

/** Тексты серой полоски под шапкой — один источник правды по формулировкам. */
export const DATA_STRIP_COPY = {
  loadFailedFallback: 'Показаны встроенные моки.',
  filePath: 'public/data/dashboard.snapshot.json',
  fileApplies: 'накладывается на встроенные моки.',
  builtin: 'Источник данных: встроенные моки (JSON-снимок не найден или не используется).',
} as const
