/**
 * Палитра графиков — близко к `docs/presentation-neral-exec-ai-dashboard.html` (emerald / slate).
 * Роль V в AGENTS.md: менять только здесь для согласованности всех Recharts.
 */
export const chartPalette = {
  grid: '#e2e8f0',
  axis: '#64748b',
  tickFontSize: 11,
  bar: '#059669',
  barActive: '#047857',
  tooltipBg: '#ffffff',
  tooltipBorder: '#e2e8f0',
  tooltipShadow: '0 8px 30px rgb(15 23 42 / 0.08)',
} as const
