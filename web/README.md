# Веб-дашборд группы (прототип)

Стек: **Vite 6**, **React 19**, **TypeScript**, **Tailwind CSS v4**, **React Router**, **Recharts**.

## Запуск

```bash
cd web
npm install
npm run dev
```

Откройте в браузере адрес из консоли (обычно `http://localhost:5173`).

## Данные (моки)

| Файл | Назначение |
|------|------------|
| `src/data/companies.ts` | Юрлица, выручка/прибыль/активы (как в ресёрче) |
| `src/data/financeMocks.ts` | Касса, дебиторка, кредиторка по годам (2024/2025) |

Подмена на **API** — следующий этап (`VITE_API_URL`, `fetch`).

## Скелет интерфейса

- **Шапка страницы** (`PageShell`): год, квартал (подпись; нарезка цифр по кварталам — позже), вход демо (`/login`), выход.
- **Разделы меню:** сводка, финансы (касса / дебиторка / кредиторка), **алерты** (правила из моков по году), юрлица, админ (настройки/доступы).
- **Карточка компании:** вкладки «Обзор» и «Финансы (мок)» (`?tab=finance`).
- **CSV:** кнопки на страницах дебиторки и кредиторки (клиентская выгрузка для примера).

## Контексты

- `src/context/AuthContext.tsx` — демо-пользователь в `localStorage`.
- `src/context/DashboardFiltersContext.tsx` — год и квартал для экранов.

## Сборка

```bash
npm run build
npm run preview
```

## Временная ссылка на демо (без Netlify / своего домена)

Чтобы показать результат по **HTTPS** с вашего ПК (пока работает команда):

```bash
npm run demo:share
```

Скрипт соберёт проект, поднимет превью на порту **4173** и выдаст случайный адрес вида `https://….loca.lt`. Остановка — **Ctrl+C**.  
Иногда при первом заходе localtunnel показывает страницу с кнопкой **Continue** — это нормально.

Требуется выход в интернет; файлы проекта остаются локально, в облако ничего не загружается.

## Статический хостинг GitHub Pages (бесплатный `github.io`)

Постоянная ссылка без Netlify: после включения **Pages → GitHub Actions** в настройках репозитория адрес будет  
`https://konstantinoviceduard9-bit.github.io/agrocenter/` (см. `docs/deploy-dashboard-preview.md`).
