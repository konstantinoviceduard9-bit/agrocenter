# Деплой из корня репозитория: используйте netlify.toml в корне (base = "web").
# Этот файл оставлен для справки при отдельном репозитории только с папкой web.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
